<div class="question-group">
	<div class="form-group">
		<label>Type</label>

		<select onchange={{action (mut selectedType) value="target.value"}}>
			{{#each types key="@index" as |type|}}
				<option value={{type.value}} selected={{is-equal type selectedType}}>
					{{type.label}}
				</option>
			{{/each}}
		</select>
	</div>

	{{component questionComponent}}
</div>